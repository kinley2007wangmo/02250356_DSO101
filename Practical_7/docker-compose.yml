version: "3"

services:

  app:

    build: .

    container_name: practical7_app

  nginx:

    image: nginx:latest

    container_name: practical7_nginx

    ports:
      - "8084:80"

    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro

    depends_on:
      - app